import type React from 'react'

interface SpriteIconProps {
	iconName: string
	width?: React.CSSProperties['width']
	height?: React.CSSProperties['height']
	classNames?: string[]
}

export default SpriteIconProps
